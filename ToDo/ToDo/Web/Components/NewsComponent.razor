@inherits AppComponentBase;

<div class="news-component-container">
    <h1>NewsComponent this is title = @newsModel.Title</h1>
    <h3>NewsComponent this is des = @newsModel.Description</h3>
    <BitButton ButtonStyle="BitButtonStyle.Primary"
               OnClick="WrapHandled(OnRemoveButton)"
               Title="@Localizer[nameof(AppStrings.Remove)]"
               AriaLabel="@Localizer[nameof(AppStrings.Remove)]">
               Remove
    </BitButton>
</div>

